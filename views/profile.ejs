<link rel="stylesheet" href="/css/profile.css">


<div id="profile-container" class="container">
    <div class="heading"><h2> Hello <%= user.name %></h2></div>
    <div class="verified"> <% if(user.isVerified){ %>
           Email Verified : <strong>Yes!</strong>
           <% }else{ %>
            Email Verified :<strong>No!</strong>
            <br>
            <p>Verification Link has been sent to your email id.</p>
            
            <% } %>
    </div>
    <div id="links">
        <a href="/users/sign-out" class="signOut-btn">Sign Out</a>
        <a href="/users/forgot-password" class="fpass-btn">Forgot Password?</a>
        <a href="/users/forgot-password">Reset Password</a>    
    </div>
    <div>
    
    </div>
</div>